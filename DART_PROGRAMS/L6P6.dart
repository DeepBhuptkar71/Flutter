void main(List<String> args) {
  print(
      "\nA DART PROGRAM TO GET THE CURRENT DATE FROM THE SYSTEM AND FORMAT INTO DIFFERENT DATE FORMATES. EX. dd/MM/yyyy, dd-MM-yyyy, dd-MMM-yyyy, dd-MM-yy, dd MMM yyyy, ETC:\n\nOUTPUT:\nCURRENT DATE AND TIME IS: ${DateTime.now()}");
}
